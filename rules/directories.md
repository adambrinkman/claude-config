# ~/code Directory Structure

- `kognic/` — repos from the `annotell` GitHub org, manually cloned when needed (Claude may clone here)
- `kognic_terraform/` — terraform repos from the `annotell` GitHub org
- `private/` — personal repos, manually cloned when needed (Claude may clone here)
  - `private/claude-config/` — Claude Code configuration (this repo)
- `public/` — public repos, manually cloned when needed (Claude may clone here)
- `tmp/` — scratch space (Claude may clone here)
